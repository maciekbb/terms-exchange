<%= simple_form_for @term, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %> 
  <%= f.input :hour %> 
  <%= f.association :day %> 
  <%= f.input :reason %> 
  <%= f.submit "Zapisz" %>

<% end %>
