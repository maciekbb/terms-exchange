<%= simple_form_for @proposal, html: {class: 'form-horizontal' } do |f| %>
	<%= f.error_notification %>

	<%= f.association :term, collection: Subject.all, as: :grouped_select, group_method: :terms %>
	<%= f.input :preferred %>
	<%= f.input :reason %>

	<%= f.button :submit %>

<% end %>
